var React = require('react-native');
var {
    View
} = React;
var Circle = React.createClass({
    render: function() {
        return (
            <View />
        );
    }

});

module.exports = Circle;